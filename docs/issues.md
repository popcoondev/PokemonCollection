# 課題メモ

動作確認で見つかった課題をこのファイルに追記して対処する。

## 使い方

- 1課題につき1項目で記録する
- `状態` は `open` / `doing` / `done` を使う
- 再現手順と期待結果を短く残す
- 修正後は `対応メモ` に変更内容を書く

## テンプレート

### 0001. 課題タイトル
- 状態: open
- 発生日: YYYY-MM-DD
- 画面/機能:
- 再現手順:
- 期待結果:
- 実際の結果:
- 対応メモ:

## 課題一覧

### 0001. ボタン押下フィードバックが遅い
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: 全体UI
- 再現手順: ページ送りや検索画面のボタンを押す
- 期待結果: タッチ直後にボタンが即ハイライトされる
- 実際の結果: 同期処理の完了までハイライト表示が遅れる、または次画面表示まで押下中の見た目が残る
- 対応メモ: タッチフィードバックと重い処理を分離する。押下表示は即時反映し、画面遷移や読込は別ステップで進める

### 0002. ページ切り替えのデータ読込が遅い
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: 図鑑詳細のページ送り
- 再現手順: 左右のページ送りボタンでポケモンを切り替える
- 期待結果: 次の図鑑ページができるだけ早く表示される
- 実際の結果: JSON と画像の読込が同期的に走り、次ページの表示まで数秒かかる
- 対応メモ: 文字情報を先に切り替え、画像は非同期または遅延描画で別更新する構成にする

### 0003. 画像読込を非同期化したい
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: 図鑑詳細 / 検索プレビュー
- 再現手順: 画像を伴う画面へ移動する
- 期待結果: 画像以外のUIは先に出て、画像は読込完了後に差し替わる
- 実際の結果: 画像読込完了まで画面更新が待たされる
- 対応メモ: プレースホルダ表示と読込完了後の差し替えが必要

### 0004. 検索機能が重く、仕様も過剰
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: SEARCH画面
- 再現手順: SEARCH画面で番号変更を続ける
- 期待結果: ポケモン番号だけを素早く切り替えられる単純な検索になる
- 実際の結果: アイコンと名称のプレビューのために都度データ参照が発生し、レスポンスが悪い
- 対応メモ: プレビュー画像と名称表示を廃止し、番号だけを切り替える軽量UIに変更した。`ひらく` を押した時だけ詳細を読む

### 0005. ページ送りのタッチ領域を透明オーバーレイ化したい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: 図鑑詳細のページ送り / 上部バー
- 再現手順: 詳細画面で左右ページ送りやSEARCHを操作する
- 期待結果: 左右ボタンは表示上は完全透明で、画面上端からタブ直上までの範囲内にある幅40px程度の透明タッチ領域として動作する。押した時だけ半透明フィードバックが出る。上部バー全体がSEARCH導線になり、専用のSEARCHボタン表示は不要
- 実際の結果: 左右のページ送りボタンが図鑑情報に重なって見える。今後この透明領域を広げるとSEARCHボタンとも干渉しやすい
- 対応メモ: ページ送りの常時表示アイコンを廃止し、左右端40pxの透明ヒットエリアと押下時オーバーレイに置き換えた。上部バー全体を検索遷移のタップ領域に変更し、SEARCH専用ボタンは廃止した

### 0006. フィードバックの透過表現が不足している
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: 押下フィードバック全般
- 再現手順: 上部バーや左右ページ送り領域を押す
- 期待結果: 後ろにある内容が透けて見える半透明フィードバックになる
- 実際の結果: 現在は半透明風の色ブレンドで、背景内容そのものは透過して見えない
- 対応メモ: スプライト構成またはアルファ合成の方法を見直す必要がある

### 0007. 左右ページ送り可能なことが見た目で分かりにくい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: 図鑑詳細のページ送り
- 再現手順: 詳細画面を見る
- 期待結果: ユーザーが一目で左右ページ送りできると分かる
- 実際の結果: 現在は透明ヒットエリアのため、触れるまで操作可能領域が分かりにくい
- 対応メモ: ポケモン名バー直下の余白に小さな `◀ ▶` ヒントを常時表示するようにした

### 0008. ポケモンタイプに色をつけたい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: タイプ表示
- 再現手順: タイプバッジを見る
- 期待結果: くさは緑、どくは紫などタイプごとの色が付く
- 実際の結果: すべて同じ色で表示されている
- 対応メモ: タイプ名ごとの色マップを追加し、バッジ背景色で表現するように変更した

### 0009. 進化タブに画像が出ない
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: しんかタブ
- 再現手順: しんかタブを開く
- 期待結果: 進化先/進化元の画像が表示される
- 実際の結果: 名前と番号だけで画像がない
- 対応メモ: 進化カード内にも画像ローダーを使って小さな画像を表示するようにした

### 0010. 進化タブから対象ポケモンへジャンプしたい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: しんかタブ
- 再現手順: しんかタブの進化先/進化元を見る
- 期待結果: 対象をタップするとそのポケモンの詳細へ移動できる
- 実際の結果: 現在は参照表示だけで遷移できない
- 対応メモ: 進化カード3枚にタップ領域を追加し、押した対象ポケモンの詳細へ遷移できるようにした

### 0011. すがたタブから全画面プレビューに遷移したい
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: すがたタブ
- 再現手順: すがたタブのポケモン画像をタップする
- 期待結果: 画像を全画面でプレビューできる
- 実際の結果: 現在は画像をタップしても拡大表示できない
- 対応メモ: 画像タップ用のヒットエリアとプレビュー専用画面を追加する

### 0012. SEARCH画面を4桁ドラムロール風にしたい
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: SEARCH画面
- 再現手順: SEARCH画面で番号を変更する
- 期待結果: 4桁それぞれを上下ボタンで操作できるドラムロール風UIになり、番号選択のアクセシビリティが高い
- 実際の結果: 現在は番号全体を `+/-` で増減するだけで、目的の番号に合わせづらい
- 対応メモ: 桁ごとの上下操作、現在選択桁の視認性、長押し時の連続変更も検討する

### 0013. 表示できない漢字が豆腐になる
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: フォント表示全般
- 再現手順: 例として ID1000 のとくせい説明を見る
- 期待結果: 「受けない」の「受」などの漢字が正しく表示される
- 実際の結果: 一部の漢字が豆腐になっている
- 対応メモ: 本文描画で `efontJA` を基本にしつつ、未収録文字だけ `efontCN` にフォールバックする混在描画に変更した

### 0014. フォントに DotGothic16 を使いたい
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: フォント表示全般
- 再現手順: 既存UIの文字表示を確認する
- 期待結果: `DotGothic16-Regular.ttf` を使って全体の文字表示を統一できる
- 実際の結果: 現在は `efontJA_10` / `efontJA_16_b` を使っており、`DotGothic16-Regular.ttf` は未使用
- 対応メモ: 低優先。SD上のTTF読込方式、サイズ、描画速度、必要文字の収録状況を確認した上で置き換え方針を決める

### 0015. ずかんのアイコン画像を約1.4倍に拡大したい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: すがたタブの画像表示
- 再現手順: ずかん詳細の画像を見る
- 期待結果: 現在より約1.4倍大きく表示される
- 実際の結果: 少し小さく見える
- 対応メモ: 画像表示領域を `140x140` に拡大し、配置も左側に寄せて表示サイズを引き上げた

### 0016. ずかんアイコン背景の丸枠を消したい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: すがたタブの画像背景
- 再現手順: ずかん詳細の画像背景を見る
- 期待結果: 画像の背後にある丸枠が表示されない
- 実際の結果: 背景に白い丸枠が表示されている
- 対応メモ: すがたタブの画像背景に描いていた円描画を削除した

### 0017. フォントを全体的に大きくしたい
- 状態: done
- 発生日: 2026-03-28
- 画面/機能: 文字表示全般
- 再現手順: 各画面のラベルや説明文を見る
- 期待結果: 解像度に合わせて全体の文字がもう少し大きく、つぶれず読みやすい
- 実際の結果: 現在は少し小さく、つぶれて見える箇所がある
- 対応メモ: 小さい文字を `efontJA_10` から `efontJA_12` に上げ、本文や補助ラベルの可読性を改善した。スペース豆腐対策として本文描画のフォントフォールバックも取り込んだ

### 0018. 進化タブからジャンプするときのフィードバックがない
- 状態: open
- 発生日: 2026-03-28
- 画面/機能: しんかタブ
- 再現手順: 進化カードをタップして別ポケモンへ移動する
- 期待結果: タップしたカードが押されたと分かる視覚フィードバックがある
- 実際の結果: 現在は遷移自体はできるが、押下時のユーザーフィードバックがない
- 対応メモ: 低優先。カード押下時のハイライトや遷移前の反応表示を追加する
